import 'package:flutter/material.dart';

class PaddingConstants {
  static PaddingConstants instance = PaddingConstants._init();

  PaddingConstants._init();

  final paddingFirstContainer = EdgeInsets.all(10);
  final paddingSecondContainer = EdgeInsets.all(10);
  final paddingYoutubeButton = EdgeInsets.all(8);
  final paddingWikipediaButton = EdgeInsets.all(8);
}
